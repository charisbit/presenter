# MLX-Audio Native macOS Dependencies

# MLX-Audio with official Kokoro support (macOS only)
mlx-audio>=0.2.3

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.11.0
numpy>=1.24.0

# Japanese text processing (required for Kokoro TTS)
misaki[ja]>=0.8.2

# Utilities
requests>=2.31.0
aiofiles>=23.0.0
huggingface_hub>=0.27.0

# MLX core and language model (pinned for compatibility)
mlx>=0.25.2
mlx-lm==0.24.0  # Pinned to avoid save_weights import issues